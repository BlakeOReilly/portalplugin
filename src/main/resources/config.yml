# Active game for this server (set by /gameset)
# Example: spleef
active-game: none

# Per-gamestate permissions granted dynamically when a player enters the state.
# Add or remove nodes to fit your server.
permissions:
  HUB:
    - minecraft.command.msg
    - essentials.nick
    - myplugin.hub.warp
  LOBBY:
    - minecraft.command.list
    - myplugin.lobby.queue
  SPLEEF:
    - myplugin.spleef.play
    - myplugin.spleef.kit.basic
  ADMIN:
    - '*'
    - portalplugin.currency.admin

# Persisted game queues created by /creategame
# Example: ["spleef", "tntrun"]
queues: []

# Persisted queue signs: world,x,y,z,game
# Example: ["world,100,65,200,spleef"]
queue-signs: []

# -------------------------------------------------------
# Server Registry (used for server auto-numbering)
# -------------------------------------------------------
server-registry:
  hostname: "127.0.0.1"

# -------------------------------------------------------
# PERSISTED HOLOGRAM LEADERBOARDS (FORMAT A)
# -------------------------------------------------------
# Each hologram is stored under holograms.<id>
# Example:
# holograms:
#   spleef_wins:
#     world: world
#     x: 100.5
#     y: 80.0
#     z: 200.5
#     gamemode: spleef
#     type: wins
#     lines:
#       - "§eTop Wins – §bSpleef"
#       - "§61. §fPlayerA §7- §a32"
#       - "§62. §fPlayerB §7- §a28"
holograms: {}
